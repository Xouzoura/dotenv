#!/bin/bash
# Nothing works at the moment with kitty and yazi.
# notify-send "Running yazi focus script"
# env > /tmp/focus_yazi_env.log
# which yazi >> /tmp/focus_yazi_env.log
# echo "Running yazi..." >> /tmp/focus_yazi_env.log
# yazi 2>> /tmp/focus_yazi_error.log
# kitty --class yazi -e yazi
# env -i PATH="$PATH" WAYLAND_DISPLAY="$WAYLAND_DISPLAY" XDG_RUNTIME_DIR="$XDG_RUNTIME_DIR" kitty -e yazi
# setsid kitty --class yazi -e yazi > /tmp/yazi.log 2>&1
# env -u WAYLAND_DISPLAY yazi > /tmp/yazi.log 2>&1
# kitty --class yazi -e ~/.local/bin/yazi-kitty.sh
